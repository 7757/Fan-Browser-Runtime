# fan-browser-runtime

Runtime implementation for Fan Browser Runtime. The current package includes a
memory runtime for tests and CLI smoke checks plus the public adapter boundary
for a future Electron/CDP implementation.
